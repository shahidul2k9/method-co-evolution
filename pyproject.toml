[tool.poetry]
name = "method-co-evolution"
version = "0.1.0"
description = "Method Co-evolution"

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"

[tool.poetry.workspace]
members = [
    "method-history-collector",
    "co-evolution"
]
